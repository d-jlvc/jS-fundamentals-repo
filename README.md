### --3. Design:

```
    # Font choice:
    --main: Yeseva One
    --side: Raleway
    
    # Color palette:
    
```
### --4. Personals:

#### --Tips:

```
    # Tooltip, font enchancer:
    
    body {
        -webkit-font-smoothing: antialiased;
        -moz-osx-smoothing: grayscale;
    }
    
    # Tooltip, font size:
    
    html {
        font-size: 62.5%;
    }
    
```
#### --CheatSheets:

```
    # CSS Responsive Logic Quick-Ref
    
    Funkcija                Šta zapravo radi?               Tipična upotreba
    ---------------------------------------------------------------------------------------------------
    max(250px, 25vw)        OSIGURAVA POD (ne može niže)    Da logo/tekst ne postane sitan na telefonu.
    min(500px, 90vw)        UDARA PLAFON (ne može više)     Da kontejner ne postane preš. na monitoru.
    clamp(min, ideal, max)  SVE U JEDNOM                    Najmoderniji način za resp. tekst i slike.
    
    # Bootstrap5 Link
    
    <link href="css/bootstrap.min.css" rel="stylesheet">
    ====================================================
    <script src="js/bootstrap.bundle.min.js"></script>
    
    #
    
```
